// import 'package:url_launcher/url_launcher.dart';
//
// void sendMessageToMessenger(String profileId, String message) async {
//   String messengerUrl = 'https://m.me/$profileId?text=${Uri.encodeFull(message)}';
//
//   if (await canLaunchUrl(Uri.parse(messengerUrl))) {
//     await launch(messengerUrl);
//   } else {
//     throw "Could not launch Messenger URL";
//   }
// }
