import 'package:flutter/material.dart';
import 'first_step_skeleton_widget.dart';

class SecondStepSkeleton extends StatelessWidget {
  const SecondStepSkeleton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const FirstStepSkeleton();
  }
}
